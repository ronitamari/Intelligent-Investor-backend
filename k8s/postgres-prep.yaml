apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-prep-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-prep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-prep
  template:
    metadata:
      labels:
        app: postgres-prep
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-prep
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-prep
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-prep
                  key: POSTGRES_DB
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-prep-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-prep
spec:
  selector:
    app: postgres-prep
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
